<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.openharness.core.persistence.mapper.MessageMapper">

    <resultMap id="messageResult" type="io.openharness.core.persistence.model.Message">
        <id property="id" column="id"/>
        <result property="sessionId" column="session_id"/>
        <result property="turnNumber" column="turn_number"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="toolName" column="tool_name"/>
        <result property="toolUseId" column="tool_use_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findBySessionId" resultMap="messageResult">
        SELECT * FROM messages WHERE session_id = #{sessionId}
        ORDER BY turn_number, created_at
    </select>

    <insert id="batchInsert">
        INSERT INTO messages (id, session_id, turn_number, role, content, tool_name, tool_use_id, created_at)
        VALUES
        <foreach collection="list" item="m" separator=",">
            (#{m.id}, #{m.sessionId}, #{m.turnNumber}, #{m.role}, #{m.content},
             #{m.toolName}, #{m.toolUseId}, #{m.createdAt})
        </foreach>
    </insert>

    <delete id="deleteBySessionId">
        DELETE FROM messages WHERE session_id = #{sessionId}
    </delete>
</mapper>
